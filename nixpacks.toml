[phases.setup]
nixPkgs = ["python311", "nodejs_22"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd spark-template && npm install && npm run build && cd .."
]

[phases.build]
cmds = []

[start]
cmd = "python server.py"
